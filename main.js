function preload(){

}

function draw(){

}

function setup(){
    canvas = createCanvas(400, 400);
    canvas.position(750,250);
    camera = createCapture(VIDEO);
    camera.size(400,400);
    camera.position(750, 650);
}